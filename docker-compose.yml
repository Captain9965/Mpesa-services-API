version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    command: python3 app.py
    volumes:
      - ./api:/api
    ports:
      - 5000:5000
    env_file:
      - ./api/.env